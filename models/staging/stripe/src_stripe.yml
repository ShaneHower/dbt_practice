version: 2

sources:
  - name: stripe
    database: pc_dbt_db
    schema: public
    tables:
      - name: payment
        loaded_at_field: _batched_at
        freshness: 
          warn_after:
            count: 12
            period: hour
          error_after:
            count: 24
            period: hour
        